# Mod Translation Toolkit v0.3.7

Existing-translation update UI hotfix.

- fixes the `.Text` error after update data is loaded
- replaces the invalid `$txtName` reference with `$txtModName`
- keeps the v0.3.6 path/drag-and-drop workflow
