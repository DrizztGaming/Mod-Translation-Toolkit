# Mod Translation Toolkit v0.2.5

PowerShell resolver hotfix.

- fixes the read-only `$PID` collision when editing existing translation mods
- keeps the multi-method original-mod resolver introduced in v0.2.4
