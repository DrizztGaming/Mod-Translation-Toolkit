# Mod Translation Toolkit v0.2.2

UI and source-copying hotfix.

- fixes the `.Text` error when choosing **Translate selected mod**
- correctly initializes the new PL/EN coverage controls
- source text can now be selected and copied directly
- `Ctrl+C` on a selected grid row copies its source text
- existing translation auto-fill remains enabled
