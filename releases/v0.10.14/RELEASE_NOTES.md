# Mod Translation Toolkit v0.10.14 Experimental

Safe Windows PowerShell 5.1 startup hotfix rebuilt from v0.10.12.

Fixes only the two invalid `$score:` interpolations and preserves valid scope variables such as `$script:UiLanguage`.
