# Mod Translation Toolkit v0.3.6

Path validation hotfix.

- fixes empty `LiteralPath` error in Update existing translation
- safely handles empty path fields
- protects pasted and dragged paths before checking them
- applies the fix to RimWorld, Kenshi and update workflow
