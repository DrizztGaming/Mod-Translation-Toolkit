# Mod Translation Toolkit v0.9.3 Experimental

## Project Zomboid Game translation progress

`Translate missing` can now handle long jobs with visible progress instead of making the UI appear frozen.

The progress bar shows:
- percent complete
- processed / total
- successful translations
- failed entries

The existing autosave checkpoint every 25 entries remains enabled.
