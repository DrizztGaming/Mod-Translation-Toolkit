# Mod Translation Toolkit v0.4.2

Lazy DLL/UI diagnostics.

- normal mod loading no longer scans DLLs
- diagnostics run only on button press
- scans active/newest content version
- skips Harmony and common dependencies
- removes noisy `GUI` matching
- shows scan status
