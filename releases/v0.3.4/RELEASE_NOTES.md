# Mod Translation Toolkit v0.3.4

Startup hotfix.

- fixes the app closing/failing to open after language selection
- repairs malformed XAML introduced in v0.3.3
- keeps the new Update existing translation workflow
